version: "2"
checks:
  similar-code:
    enabled: false
  duplication:
    enabled: true
  identical-code:
    enabled: true
plugins:
  brakeman:
    enabled: true
  bundler-audit:
    enabled: true
  rubocop:
    enabled: true
exclude_patterns:
  - .bundle/
  - reports/
  - scripts/
  - vendor/
